import { TwoFirstNamesPipe } from './two-first-names.pipe';

describe('TwoFirstNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new TwoFirstNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
